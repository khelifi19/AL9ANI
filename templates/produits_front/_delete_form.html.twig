<form id="my_form" method="post" action="{{ path('app_produits_front_delete', {'idProduct': produit.idProduct}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.idProduct) }}">
    <button class="btn">Delete</button>
</form>
