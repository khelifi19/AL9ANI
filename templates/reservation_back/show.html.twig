{% extends 'baseBack.html.twig' %}

{% block title %}Reservation{% endblock %}

{% block body %}
    <h1>Reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr>
            <tr>
                <th>Date De La Reservation</th>
                <td>{{ reservation.dateReservation ? reservation.dateReservation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Nombre De Personnes</th>
                <td>{{ reservation.nombreDePersonnes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_back_index') }}">Retourner à la liste</a>


    {{ include('reservation_back/_delete_form.html.twig') }}
{% endblock %}
