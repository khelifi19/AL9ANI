{% extends 'baseFront.html.twig' %}

{% block title %} anciennes Courses {% endblock %}

{% block content %}
<div class="container mt-5">
    <h1 class="text-center mb-4">Historique des Trajets</h1>
    <table class="table table-striped">
        <thead class="bg-danger text-white">
            <tr>
                <th>Destination</th>
                <th>Départ</th>
                <th>Date-Heure</th>
                <th>Nombre de personnes</th>
                  <th>Prix</th>
                  <th>Reservez</th>
            </tr>
        </thead>
        <tbody>
            {% for course in courses %}
            <tr>
                <td>{{ course.destination }}</td>
                <td>{{ course.depart }}</td>
                <td>{{ course.date|date('Y-m-d H:i') }}</td>
                <td>{{ course.nbPersonne }}</td>
                  <td>{{ course.prix }} D</td>
                    <td>
                    <a href="{{ path('courseUser_edit', {'id': course.id}) }}" class="btn btn-danger">Reservez A Nouveau</a>
                   
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
       <a href="{{ path('courseUser_index') }}" class="btn btn-secondary">Retour</a>
</div>
{% endblock %}

