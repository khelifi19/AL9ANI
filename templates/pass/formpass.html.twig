{% extends "base.html.twig" %}
{% block body %}
<style>
.align-center {
            text-align: center;
        }

          .align-right {

            text-align: right;}
</style>
  <body
     class="align-center"
        <br> <br> <br>
        <section class="space min">
            <div class="container">
                <div class="row justify-content-center">
                    <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12">
                        <div class="sec_title position-relative text-center mb-5">
                            <h2 class="ft-bold">Ajouter un pass</h2>
                        </div>
                    </div>
                </div>

                <div class="row justify-content-center">
                    <div class="col-md-6">
                        {{ form_start(form) }}
                        {{ form_errors(form) }}

                        {{ form_row(form.prixPass) }}<br>
                        {{ form_row(form.type) }}<br>

                        <div class="text-center">
                            <button type="submit" class="btn btn-primary">
                                Confirm
                            </button>
                        </div>

                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </section>
    </body>
{% endblock %}